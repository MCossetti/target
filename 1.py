# Inicializa as variaveis
indice = 13
soma = 0
k = 0

# Executa o loop enquanto `k` for menor que `indice`
while (k < indice):
    k += 1
    soma += k

# Após o loop, imprime o valor final de `soma`
print(soma)

# O valor de `soma` é 91, pois o loop soma os números de 1 a 13